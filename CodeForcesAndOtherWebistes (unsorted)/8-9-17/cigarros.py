import sys
for inp_l in sys.stdin.read():
	n,k = map(int, inp_l.split(" "))
	i = 0
	while n!= 0:
		new = int(n/k
		leftovers = n%k	
		i += (n - leftovers)
		n = (old + leftovers)
	print(str(i), end+"")
